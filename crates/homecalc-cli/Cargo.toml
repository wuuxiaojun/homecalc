[package]
name = "homecalc-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
mortgage = { path = "../mortgage" }
loc = { path = "../loc" }
crossterm = "0.27"
inquire = "0.7"
chrono = { version = "0.4", features = ["serde"] }
